// tslint:disable-next-line: no-namespace
declare namespace __MaterialUI {
  export interface IconButtonProps {
    // Append `rel` property to icon buttons.
    // It exists but undocumented. Should equal to `rel` in `a` element
    rel?: string;
  }
}
